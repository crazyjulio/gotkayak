%h1 #{@manufacturer.name} Product Features
%br
%p
  Notes: If an item has a price, it is an optional feature. If there is no price, it is a standard feature.
%br
%table.itunes_table
  %thead
    %tr
      %th{:scope => "col"} Name
      %th{:scope => "col"} Description
      %th{:scope => "col"} Price
      %th{:scope => "col"} Manufacturer
      %th{:scope => "col"}
      %th{:scope => "col"}
      %th{:scope => "col"}

  - @manufacturer.features.each_with_index do |feature,i|
    - if i.odd?
      %tr.odd
        %td= feature.name
        %td= feature.description
        - if !feature.price.blank? && feature.price != 0
          %td= number_to_currency feature.price
        - else
          %td
        %td= feature.manufacturer.name
        %td= link_to 'Show', feature
        %td= link_to 'Edit', edit_feature_path(feature)
        %td= link_to 'Destroy', feature, :confirm => 'Are you sure?', :method => :delete
    - else
      %tr
        %td= feature.name
        %td= feature.description
        - if !feature.price.blank? && feature.price != 0
          %td= number_to_currency feature.price
        - else
          %td
        %td= feature.manufacturer.name
        %td= link_to 'Show', feature
        %td= link_to 'Edit', edit_feature_path(feature)
        %td= link_to 'Destroy', feature, :confirm => 'Are you sure?', :method => :delete
.clearboth
%br

= link_to 'New Feature', :controller => :features, :action => :new, :manufacturer_id => @manufacturer.id
\|
= link_to "Go to #{@manufacturer.name}", :controller => :manufacturers, :action => :show, :id => @manufacturer.id